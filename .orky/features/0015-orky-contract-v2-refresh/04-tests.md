# Tests — 0015-orky-contract-v2-refresh

**Phase:** 4 (tests). **Inputs:** `02-spec.md` (REQ-101..REQ-112), `03-plan.md` (TASK-101..TASK-114).
Written BEFORE implementation (frozen once this gate passes, ADR-009). The implementer changes
`src/` (and regenerates fixtures / reconciles docs) until these go green — never the tests.

## TEST → REQ map

| TEST | REQ(s) | File | Assertion |
|---|---|---|---|
| TEST-707 | REQ-101, REQ-104 | `tests/main/orky-cli-contract-handshake.test.ts` | `EXPECTED_CONTRACT_VERSION === 2` (strict pin), and a fake runner emitting `contract_version: 2` + `[...ORKY_PHASES]` returns exactly `{ ok: true, contractVersion: 2, mismatches: [] }` with ZERO warns; the invocation pins (`['contract']` args, `CONTRACT_HANDSHAKE_TIMEOUT_MS`) hold. |
| TEST-708 | REQ-102 | `tests/main/orky-cli-contract-handshake.test.ts` | The INVERTED mixed-fleet case: `contract_version: 1` → `ok:false`, `contractVersion === 1`, exactly ONE mismatch containing `contract_version`, `plugin=1`, `expected=${EXPECTED_CONTRACT_VERSION}`; exactly one warn line containing `plugin=1` and the literal `expected=2`. Log-only (the no-caller-gates-on-`.ok` half is a review-time grep, unchanged from today). |
| TEST-709 | REQ-103 | `tests/main/orky-cli-contract-handshake.test.ts` | Future version: `contract_version: 3` → `ok:false`, `contractVersion === 3`, one both-sides mismatch (`plugin=3`, `expected=${EXPECTED_CONTRACT_VERSION}`), one warn — REQ-102's shape, newer side. |
| TEST-710 | REQ-104 | `tests/main/orky-cli-contract-handshake.test.ts` | The same-length phase RENAME (`[...ORKY_PHASES.slice(0,-1), 'human']`) is still drift — assertions byte-identical to the pre-0015 case (v2 did not change `phases`). REQ-104's full breadth additionally rides the UNTAGGED, unchanged cases in the same file: never-throw/tolerant degradation (absent CLI, pre-`contract` plugin, non-JSON, timeout, rejecting runner), per-path cache + single warn + shared in-flight promise, inserted-phase drift, combined version+phase drift — only version literals inside `goodContract` overrides were touched, per the spec's allowance. |
| TEST-711 | REQ-105 | `tests/shared/orky-contract-golden.test.ts` | The COMMITTED `contract.json` is the regenerated v2 snapshot: `contract_version === 2`, `phase_order` starts `'intake'` and ends `'human-review'`, top-level `finding_resolution` (object) and `producer_tiers` keys present, `state.gate_fields` contains `'firstAt'`. Fixtures must be regenerated by ONE run of `tools/generate-orky-contract-fixtures.mjs` against plugin 0.30.0 (TASK-103) — never hand-edited to satisfy this. |
| TEST-712 | REQ-106 | `tests/shared/orky-contract-golden.test.ts` | `contract.phases` still deep-equals `ORKY_PHASES` (8 entries, ending `human-review`) AND the exported `ORKY_PHASES` literal block in `src/shared/orky-status.ts` is textually unchanged (EOL-normalized byte pin). Green today and MUST stay green after regeneration. The pre-existing untagged golden assertions (phases deep-equal, OSC code/cap, state/active normalization) also back REQ-105(b)/REQ-106 and were not modified. |
| TEST-713 | REQ-109 | `tests/main/orky-root-detail-resolution.test.ts` | `mapFinding` (via `assembleOrkyRootDetail`) maps string `resolution`/`resolvedBy` VERBATIM (padding intact — no trimming) and `resolvedAt` tz-safe to epoch ms via the escalation-mapper parse precedent (tz-less ISO ≡ explicit-Z twin). |
| TEST-714 | REQ-109 | `tests/main/orky-root-detail-resolution.test.ts` | Total mapping: absent / `null` / mistyped (number resolution, object resolvedBy, junk string or NUMERIC resolvedAt) → all three fields `null`, no throw; every PRE-EXISTING `OrkyFindingDetail` field (id/severity/status/claim/blocking incl. the shared predicate) maps exactly as before. |
| TEST-715 | REQ-110 | `tests/renderer/orky-pane-resolution-structure.test.ts` | Structural (node-env source scan, the `orky-pane-display-contract` precedent): `OrkyPane.tsx` finding rows consume `.resolution`/`.resolvedBy`/`.resolvedAt`, render the `— resolution:` affix behind a CASE-INSENSITIVE resolved check (at most the escalation row's one strict `=== 'resolved'` site survives) plus a `resolution !== null` guard, and format the carried `resolvedAt` epoch via `formatOrkyInstant` — no renderer clock (`Date.now`/`new Date` still banned in the file). |
| TEST-716 | REQ-110 (acceptance a+b) | `tests/e2e/orky-pane-resolution.spec.ts` | Rendered: a `status:'resolved'` finding with `resolution:'rewrote the guard'`, `resolvedBy:'kevin'`, `resolvedAt` renders the `— resolution: rewrote the guard` affix on its `orky-pane-finding` row with `kevin` + the formatted instant (`2026-07-01 00:00:00 UTC`) observable in row text or `title`; a MIXED-CASE `'Resolved'` status shows its affix too. |
| TEST-717 | REQ-110 (acceptance c) | `tests/e2e/orky-pane-resolution.spec.ts` | Rendered: an open finding and a resolved finding with no resolution render EXACTLY today's row content (`toHaveText` exact pins: `F-OPEN MEDIUM open a medium note` / `F-BARE LOW resolved resolved but bare`) with NO `resolution:` substring in text or `title` — never `— resolution: null`. |
| TEST-718 | REQ-111 | `tests/shared/orky-status-v2-blocking.test.ts` | A v2-shaped resolved finding (`status:'resolved'`/`'Resolved'`, `severity:'CRITICAL'`, resolution fields present, even `contract_violation:true`) yields `isBlockingFinding(...) === false`; `openBlockingCount` over a mixed list counts only the open-blocking control entry. **Deliberately GREEN today** — it pins the untouched predicates' behavior over the new shape (the guard against "adapting" them for v2). The `git diff`-no-change half is review-time (TASK-113), backed by the FROZEN `orky-status-blocking` / characterization suites passing unmodified. |
| TEST-719 | REQ-107, REQ-105(c) | `tests/docs-feature-0015.test.ts` | Positive presence pins over the spec's NAMED sites (never a repo-wide absence sweep — the CONV-023 greps stay the review-time authority, TASK-109): `src/shared/orky-status.ts` + `docs/features/orky-status.md` § Data provenance quote `PHASE_ORDER` ending `'human-review'` (the stale `…,'doc-sync','human']` quote gone), keep the live intake-excluded warning + the do-not-re-sync instruction, and record the rename as historical/resolved-at-contract-v2; `docs/features/orky-action-dispatch.md` says expected `` `2` ``, not `` `1` ``; CHANGELOG `[Unreleased]` records plugin `0.30.0` + the generator command (REQ-105(c) auditability, TASK-104). |
| TEST-691 *(existing — UNMODIFIED, referenced)* | REQ-108 | `tests/integration/orky-act-loop.test.ts` | The terminal acceptance criterion: the LIVE `gatekeeper contract` handshake against the installed plugin at `ORKY_PLUGIN_DIR` returns `ok:true`, `contractVersion === EXPECTED_CONTRACT_VERSION`, `mismatches: []`, `note` undefined, zero warns. Its assertion body MUST NOT be edited (verified by `git diff` at TASK-114); currently RED with plugin 0.30.0 installed — it goes green when TASK-101+TASK-103 land. Self-skips without the plugin; the plugin-present run is the acceptance run. |
| TEST-702 *(existing — UNMODIFIED, referenced)* | REQ-112 | `tests/integration/orky-act-loop.test.ts` | The read/write-boundary invariant this feature must not disturb: the changed-file set across every `.orky/` tree is exactly the sanctioned CLI-written set. Passes unmodified. |

## Coverage

| REQ | Tests |
|---|---|
| REQ-101 | TEST-707 |
| REQ-102 | TEST-708 |
| REQ-103 | TEST-709 |
| REQ-104 | TEST-707, TEST-710 (+ the unchanged untagged invariant cases in the handshake file) |
| REQ-105 | TEST-711, TEST-719 (+ TEST-691's live version+phases subset) |
| REQ-106 | TEST-712 |
| REQ-107 | TEST-719 (named-site pins; the exhaustive CONV-023 greps run at review, TASK-109) |
| REQ-108 | TEST-691 (existing, unmodified — terminal acceptance) |
| REQ-109 | TEST-713, TEST-714 |
| REQ-110 | TEST-715, TEST-716, TEST-717 |
| REQ-111 | TEST-718 (green-by-design invariant guard) + review-time `git diff` |
| REQ-112 | TEST-702 (existing, unmodified) + review-time scope greps (`rg "producer_tiers|producerTier" src`, no `src/main/ipc/` channel changes, no `SCHEMA_VERSION` change) — deliberately NOT new frozen absence-guards (spec's explicit CONV-019/CONV-037 avoidance) |

Uncovered REQs: none.

## RED-run evidence (2026-07-03, pristine tree — no implementation)

`npx vitest run` over the six unit suites: **15 failed / 18 passed**, `npm run typecheck` fully
green (every failure is assertion-level, none compile-level — the not-yet-existing
`OrkyFindingDetail` fields are read through tolerant accessors, the frozen TEST-392 `slugOf`
precedent).

- `tests/main/orky-cli-contract-handshake.test.ts` — 7 failed / 10 passed. TEST-707 + the healthy
  extra-fields case + both goodContract-based cache cases + the concurrent case fail because
  `EXPECTED_CONTRACT_VERSION` is still 1 (a v2 feed mismatches: warn `plugin=2 expected=1`);
  TEST-708 fails because a v1 feed still handshakes clean (`ok:true`); inserted-phase and combined
  cases fail on the extra version mismatch. TEST-709/TEST-710 and the whole degradation block pass
  (version-agnostic invariants).
- `tests/shared/orky-contract-golden.test.ts` — 1 failed / 7 passed. TEST-711 fails on the
  committed v1 fixture (`contract_version` 1, `phase_order` ends `'human'`, no
  `finding_resolution`/`producer_tiers`/`firstAt`). TEST-712 and all pre-existing assertions pass.
- `tests/main/orky-root-detail-resolution.test.ts` — 2 failed / 0 passed. TEST-713/TEST-714:
  `mapFinding` maps none of the three fields (`undefined` where a verbatim value / explicit `null`
  is required).
- `tests/renderer/orky-pane-resolution-structure.test.ts` — 1 failed. TEST-715: no affix, no
  resolvedBy/resolvedAt consumption, no case-insensitive resolved check in `OrkyPane.tsx`.
- `tests/shared/orky-status-v2-blocking.test.ts` — 1 passed (TEST-718, green by design — see above).
- `tests/docs-feature-0015.test.ts` — 4 failed. TEST-719: both provenance sites still quote
  `…,'doc-sync','human']`, `orky-action-dispatch.md` still claims expected `` `1` ``, no `0.30.0`
  provenance in `[Unreleased]`.
- `tests/e2e/orky-pane-resolution.spec.ts` (TEST-716/TEST-717) compiles under `tsconfig.e2e.json`
  (typecheck green) but was NOT executed in this phase — e2e runs against a built `out/` and is
  outside the `npm test` gate; against the shipped OrkyPane it is RED by construction (no finding
  row renders any `— resolution:` affix). It runs at the implement/review verification alongside
  the rest of the Playwright suite (`npm run build && npm run e2e`).
- `tests/integration/orky-act-loop.test.ts` TEST-691 — already RED on a plugin-0.30.0 machine
  (live `contract_version` 2 vs the pin 1); NOT modified in this phase, per REQ-108.

## Frozen-test edits sanctioned by this phase (the spec's baseline note)

- `tests/main/orky-cli-contract-handshake.test.ts`: `goodContract` baseline 1→2 (TASK-102);
  "v2 → mismatch" INVERTED to TEST-708's v1 case; version literals inside `goodContract` overrides
  adjusted in place (`contractVersion` expectation 1→2 in the inserted-phase case; the combined
  drift case feeds 1 instead of 2; the cached-mismatch case keeps 3). No other assertion changed.
- `tests/shared/orky-contract-golden.test.ts`: additive describe-block only (TEST-711/TEST-712);
  every pre-existing assertion byte-for-byte unchanged.
- NOT touched: `tests/integration/orky-act-loop.test.ts` (TEST-691/TEST-702), every 0004..0014
  frozen suite, `tests/fixtures/orky-contract/*` (regeneration is TASK-103, implementer, via the
  generator ONLY), `ORKY_PHASES`, all `src/`.

## Notes for the implementer

- The fixtures turning TEST-711 green MUST come from one run of
  `node tools/generate-orky-contract-fixtures.mjs` against plugin 0.30.0 — a hand-edited fixture
  violates binding decision 3 even if it makes the test pass.
- TEST-715 allows at most ONE strict `status === 'resolved'` in `OrkyPane.tsx` — that budget is the
  EXISTING escalation-row check; write the finding-row check case-folded.
- TEST-717 pins the open / bare-resolved rows' EXACT text — add nothing (not even trailing
  whitespace-visible artifacts) to non-affixed rows.
- TEST-716 pins the formatted instant `2026-07-01 00:00:00 UTC` — `formatOrkyInstant` output for
  the seeded epoch; surface it in the row text or the row's `title`, either satisfies.

## TASK-103 execution record (tests-phase re-entry, per amended 03-plan.md)

- 2026-07-03: fixtures regenerated by ONE run of
  `ORKY_PLUGIN_DIR=C:\dev\Orky\plugin node tools/generate-orky-contract-fixtures.mjs`
  against the installed Orky plugin **0.32.0** (`C:/dev/Orky/plugin`, commit `1c59d74`) — plugin
  had moved past the 0.30.0 named at spec time; the emitted contract is still `contract_version: 2`
  with the same v2 shape, so all TEST-711/TEST-712 pins hold unchanged. No hand-edits. Verified:
  `contract_version === 2`, 8-entry `phases` ≡ `ORKY_PHASES`, `phase_order` `intake`→`human-review`,
  `finding_resolution` + `producer_tiers` present, `state.gate_fields` contains `firstAt`.
  Golden suite GREEN unmodified (8/8); feature suite still RED: handshake 7f/10p,
  root-detail-resolution 2f/0p, pane-resolution-structure 1f/0p, docs-0015 4f/0p,
  v2-blocking 0f/1p (green by design).
