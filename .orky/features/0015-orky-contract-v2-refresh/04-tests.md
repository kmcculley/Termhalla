# Tests — 0015-orky-contract-v2-refresh

**Phase:** 4 (tests). **Inputs:** `02-spec.md` (REQ-101..REQ-112), `03-plan.md` (TASK-101..TASK-114).
Written BEFORE implementation (frozen once this gate passes, ADR-009). The implementer changes
`src/` (and regenerates fixtures / reconciles docs) until these go green — never the tests.

> **AMENDED 2026-07-03 — ESC-001 descent, tests-phase re-entry (TASK-115/116/117).** The spec was
> amended (REQ-105/107/110 amended, REQ-113 added). Exactly three test files were amended through
> the sanctioned tests-phase path: `tests/docs-feature-0015.test.ts` (TEST-719 re-pinned
> version-agnostic; TEST-720/TEST-721 added), `tests/renderer/orky-pane-resolution-structure.test.ts`
> (TEST-722 added), `tests/e2e/orky-pane-resolution.spec.ts` (TEST-723/TEST-724 added; the shared
> seed gained the F-EMPTY vector, finding count 4→5). No other test redesigned; no fixture touched;
> no `src/` change. See the descent record at the end of this file.

## TEST → REQ map

| TEST | REQ(s) | File | Assertion |
|---|---|---|---|
| TEST-707 | REQ-101, REQ-104 | `tests/main/orky-cli-contract-handshake.test.ts` | `EXPECTED_CONTRACT_VERSION === 2` (strict pin), and a fake runner emitting `contract_version: 2` + `[...ORKY_PHASES]` returns exactly `{ ok: true, contractVersion: 2, mismatches: [] }` with ZERO warns; the invocation pins (`['contract']` args, `CONTRACT_HANDSHAKE_TIMEOUT_MS`) hold. |
| TEST-708 | REQ-102 | `tests/main/orky-cli-contract-handshake.test.ts` | The INVERTED mixed-fleet case: `contract_version: 1` → `ok:false`, `contractVersion === 1`, exactly ONE mismatch containing `contract_version`, `plugin=1`, `expected=${EXPECTED_CONTRACT_VERSION}`; exactly one warn line containing `plugin=1` and the literal `expected=2`. Log-only (the no-caller-gates-on-`.ok` half is a review-time grep, unchanged from today). |
| TEST-709 | REQ-103 | `tests/main/orky-cli-contract-handshake.test.ts` | Future version: `contract_version: 3` → `ok:false`, `contractVersion === 3`, one both-sides mismatch (`plugin=3`, `expected=${EXPECTED_CONTRACT_VERSION}`), one warn — REQ-102's shape, newer side. |
| TEST-710 | REQ-104 | `tests/main/orky-cli-contract-handshake.test.ts` | The same-length phase RENAME (`[...ORKY_PHASES.slice(0,-1), 'human']`) is still drift — assertions byte-identical to the pre-0015 case (v2 did not change `phases`). REQ-104's full breadth additionally rides the UNTAGGED, unchanged cases in the same file: never-throw/tolerant degradation (absent CLI, pre-`contract` plugin, non-JSON, timeout, rejecting runner), per-path cache + single warn + shared in-flight promise, inserted-phase drift, combined version+phase drift — only version literals inside `goodContract` overrides were touched, per the spec's allowance. |
| TEST-711 | REQ-105 | `tests/shared/orky-contract-golden.test.ts` | The COMMITTED `contract.json` is the regenerated v2 snapshot: `contract_version === 2`, `phase_order` starts `'intake'` and ends `'human-review'`, top-level `finding_resolution` (object) and `producer_tiers` keys present, `state.gate_fields` contains `'firstAt'`. Fixtures must be regenerated by ONE run of `tools/generate-orky-contract-fixtures.mjs` against the installed v2-contract plugin (TASK-103; the executed run recorded 0.32.0 / commit `1c59d74`) — never hand-edited to satisfy this. |
| TEST-712 | REQ-106 | `tests/shared/orky-contract-golden.test.ts` | `contract.phases` still deep-equals `ORKY_PHASES` (8 entries, ending `human-review`) AND the exported `ORKY_PHASES` literal block in `src/shared/orky-status.ts` is textually unchanged (EOL-normalized byte pin). Green today and MUST stay green after regeneration. The pre-existing untagged golden assertions (phases deep-equal, OSC code/cap, state/active normalization) also back REQ-105(b)/REQ-106 and were not modified. |
| TEST-713 | REQ-109 | `tests/main/orky-root-detail-resolution.test.ts` | `mapFinding` (via `assembleOrkyRootDetail`) maps string `resolution`/`resolvedBy` VERBATIM (padding intact — no trimming) and `resolvedAt` tz-safe to epoch ms via the escalation-mapper parse precedent (tz-less ISO ≡ explicit-Z twin). |
| TEST-714 | REQ-109 | `tests/main/orky-root-detail-resolution.test.ts` | Total mapping: absent / `null` / mistyped (number resolution, object resolvedBy, junk string or NUMERIC resolvedAt) → all three fields `null`, no throw; every PRE-EXISTING `OrkyFindingDetail` field (id/severity/status/claim/blocking incl. the shared predicate) maps exactly as before. |
| TEST-715 | REQ-110 | `tests/renderer/orky-pane-resolution-structure.test.ts` | Structural (node-env source scan, the `orky-pane-display-contract` precedent): `OrkyPane.tsx` finding rows consume `.resolution`/`.resolvedBy`/`.resolvedAt`, render the `— resolution:` affix behind a CASE-INSENSITIVE resolved check (at most the escalation row's one strict `=== 'resolved'` site survives) plus a `resolution !== null` guard, and format the carried `resolvedAt` epoch via `formatOrkyInstant` — no renderer clock (`Date.now`/`new Date` still banned in the file). |
| TEST-716 | REQ-110 (acceptance a+b) | `tests/e2e/orky-pane-resolution.spec.ts` | Rendered: a `status:'resolved'` finding with `resolution:'rewrote the guard'`, `resolvedBy:'kevin'`, `resolvedAt` renders the `— resolution: rewrote the guard` affix on its `orky-pane-finding` row with `kevin` + the formatted instant (`2026-07-01 00:00:00 UTC`) observable in row text or `title`; a MIXED-CASE `'Resolved'` status shows its affix too. |
| TEST-717 | REQ-110 (acceptance c) | `tests/e2e/orky-pane-resolution.spec.ts` | Rendered: an open finding and a resolved finding with no resolution render EXACTLY today's row content (`toHaveText` exact pins: `F-OPEN MEDIUM open a medium note` / `F-BARE LOW resolved resolved but bare`) with NO `resolution:` substring in text or `title` — never `— resolution: null`. |
| TEST-718 | REQ-111 | `tests/shared/orky-status-v2-blocking.test.ts` | A v2-shaped resolved finding (`status:'resolved'`/`'Resolved'`, `severity:'CRITICAL'`, resolution fields present, even `contract_violation:true`) yields `isBlockingFinding(...) === false`; `openBlockingCount` over a mixed list counts only the open-blocking control entry. **Deliberately GREEN today** — it pins the untouched predicates' behavior over the new shape (the guard against "adapting" them for v2). The `git diff`-no-change half is review-time (TASK-113), backed by the FROZEN `orky-status-blocking` / characterization suites passing unmodified. |
| TEST-719 *(amended, ESC-001 — TASK-115)* | REQ-107, REQ-105(c) | `tests/docs-feature-0015.test.ts` | Positive presence pins over the spec's NAMED sites (never a repo-wide absence sweep — the CONV-023 greps stay the review-time authority, TASK-109): `src/shared/orky-status.ts` + `docs/features/orky-status.md` § Data provenance quote `PHASE_ORDER` ending `'human-review'` (the stale `…,'doc-sync','human']` quote gone), keep the live intake-excluded warning + the do-not-re-sync instruction, and record the rename as historical/resolved-at-contract-v2; `docs/features/orky-action-dispatch.md` says expected `` `2` ``, not `` `1` ``; CHANGELOG `[Unreleased]` — **amended**: the `toContain('0.30.0')` pin is DROPPED (amended REQ-105 forbids requiring a hardcoded version numeral); the provenance must instead match the version-agnostic shape `regenerated against plugin <semver> (commit <sha>)` (markdown bold/backticks tolerated) — the generation-time recorded producer (0.32.0 / `1c59d74`) leading as fact — plus the auditable generator command. `0.30.0` may survive only as a historical mention and is neither required nor forbidden. |
| TEST-720 *(new, ESC-001 — TASK-117)* | REQ-107 (amended, scan 4) | `tests/docs-feature-0015.test.ts` | The prose-phrasing scan encoded FROZEN (the descent's RED signal for CLAUDE.md:179 and a durable drift guard): every `/intake.{1,5}human/` hit in `CLAUDE.md`, `src/shared/orky-status.ts`, and `docs/features/orky-status.md` must either continue `-review` or carry an explicit historical/pre-v2 label within ±200 chars — in particular CLAUDE.md's "(intake…human)" parenthetical at line ~179 no longer passes. Scans (1)–(3) remain review-time greps (TASK-109/TASK-122); historical artifacts stay exempt and are never read. |
| TEST-721 *(new, ESC-001 — TASK-117, optional REQ-113 pin exercised)* | REQ-113 | `tests/docs-feature-0015.test.ts` | `docs/features/orky-pane.md` documents the v2 resolution display: names all three `OrkyFindingDetail` fields (`` `resolution` ``/`` `resolvedBy` ``/`` `resolvedAt` ``) and, within ±2000 chars of the `— resolution:` affix mention (CONV-032 anchor — one contiguous section), names BOTH display guards (`case-insensitiv…`, `non-empty`) and the row-title/tooltip full-text mirror. |
| TEST-722 *(new, ESC-001 — TASK-116)* | REQ-110 (amended) | `tests/renderer/orky-pane-resolution-structure.test.ts` | Structural half of the two new MUSTs: the display guard also excludes the EMPTY string (`/resolution\s*!==\s*(''\|"")/` — the spec names that exact guard shape) AND the finding row's open tag (sliced testid→`>`, CONV-032-anchored) keeps a `title={` attribute that is no longer the unconditional bare `title={fd.claim}` — the affixed row's full text must be reachable via the title. |
| TEST-723 *(new, ESC-001 — TASK-116)* | REQ-110 (amended acceptance a) | `tests/e2e/orky-pane-resolution.spec.ts` | Rendered title mirror: whenever the affix renders, the row's `title` contains the claim PLUS the resolution details — F-LOWER's title carries `guard was wrong` + `rewrote the guard` + `kevin` + `2026-07-01 00:00:00 UTC`; F-MIXED (no resolvedBy/resolvedAt) still mirrors claim + its shorter affix (`mixed case entry` + `case-folded fix`). The `by <resolvedBy>` LABEL is a MAY and is deliberately NOT pinned (FINDING-002). |
| TEST-724 *(new, ESC-001 — TASK-116)* | REQ-110 (amended acceptance d) | `tests/e2e/orky-pane-resolution.spec.ts` | Rendered empty-string guard: the new F-EMPTY vector (`status:'resolved'`, `resolution:''`, non-null `resolvedBy`/`resolvedAt` — the mapper keeps `''` verbatim) renders EXACTLY the no-affix content (`toHaveText` pin `F-EMPTY LOW resolved resolved but empty`), NO `resolution:` substring in text or title, and no orphaned parts — `kevin` and the formatted instant must NOT leak onto the row's observable surface. |
| TEST-691 *(existing — UNMODIFIED, referenced)* | REQ-108 | `tests/integration/orky-act-loop.test.ts` | The terminal acceptance criterion: the LIVE `gatekeeper contract` handshake against the installed plugin at `ORKY_PLUGIN_DIR` returns `ok:true`, `contractVersion === EXPECTED_CONTRACT_VERSION`, `mismatches: []`, `note` undefined, zero warns. Its assertion body MUST NOT be edited (verified by `git diff` at TASK-114/122). Self-skips without the plugin; the plugin-present run is the acceptance run — never conditioned on a specific release numeral (amended REQ-108). |
| TEST-702 *(existing — UNMODIFIED, referenced)* | REQ-112 | `tests/integration/orky-act-loop.test.ts` | The read/write-boundary invariant this feature must not disturb: the changed-file set across every `.orky/` tree is exactly the sanctioned CLI-written set. Passes unmodified. |

## Coverage

| REQ | Tests |
|---|---|
| REQ-101 | TEST-707 |
| REQ-102 | TEST-708 |
| REQ-103 | TEST-709 |
| REQ-104 | TEST-707, TEST-710 (+ the unchanged untagged invariant cases in the handshake file) |
| REQ-105 | TEST-711, TEST-719 (amended — version-agnostic provenance shape) (+ TEST-691's live version+phases subset) |
| REQ-106 | TEST-712 |
| REQ-107 | TEST-719 (named-site pins), TEST-720 (frozen prose-phrasing scan 4); the exhaustive CONV-023 greps (1)–(3) run at review, TASK-109/TASK-122 |
| REQ-108 | TEST-691 (existing, unmodified — terminal acceptance) |
| REQ-109 | TEST-713, TEST-714 |
| REQ-110 | TEST-715, TEST-716, TEST-717, TEST-722 (structural: non-empty guard + title-not-bare-claim), TEST-723 (rendered title mirror), TEST-724 (rendered empty-string guard) |
| REQ-111 | TEST-718 (green-by-design invariant guard) + review-time `git diff` |
| REQ-112 | TEST-702 (existing, unmodified) + review-time scope greps (`rg "producer_tiers|producerTier" src`, no `src/main/ipc/` channel changes, no `SCHEMA_VERSION` change) — deliberately NOT new frozen absence-guards (spec's explicit CONV-019/CONV-037 avoidance) |
| REQ-113 | TEST-721 (doc pins — the spec's "docs test" acceptance option, exercised per TASK-117's discretion clause) |

Uncovered REQs: none.

## RED-run evidence (2026-07-03, pristine tree — no implementation)

`npx vitest run` over the six unit suites: **15 failed / 18 passed**, `npm run typecheck` fully
green (every failure is assertion-level, none compile-level — the not-yet-existing
`OrkyFindingDetail` fields are read through tolerant accessors, the frozen TEST-392 `slugOf`
precedent).

- `tests/main/orky-cli-contract-handshake.test.ts` — 7 failed / 10 passed. TEST-707 + the healthy
  extra-fields case + both goodContract-based cache cases + the concurrent case fail because
  `EXPECTED_CONTRACT_VERSION` is still 1 (a v2 feed mismatches: warn `plugin=2 expected=1`);
  TEST-708 fails because a v1 feed still handshakes clean (`ok:true`); inserted-phase and combined
  cases fail on the extra version mismatch. TEST-709/TEST-710 and the whole degradation block pass
  (version-agnostic invariants).
- `tests/shared/orky-contract-golden.test.ts` — 1 failed / 7 passed. TEST-711 fails on the
  committed v1 fixture (`contract_version` 1, `phase_order` ends `'human'`, no
  `finding_resolution`/`producer_tiers`/`firstAt`). TEST-712 and all pre-existing assertions pass.
- `tests/main/orky-root-detail-resolution.test.ts` — 2 failed / 0 passed. TEST-713/TEST-714:
  `mapFinding` maps none of the three fields (`undefined` where a verbatim value / explicit `null`
  is required).
- `tests/renderer/orky-pane-resolution-structure.test.ts` — 1 failed. TEST-715: no affix, no
  resolvedBy/resolvedAt consumption, no case-insensitive resolved check in `OrkyPane.tsx`.
- `tests/shared/orky-status-v2-blocking.test.ts` — 1 passed (TEST-718, green by design — see above).
- `tests/docs-feature-0015.test.ts` — 4 failed. TEST-719: both provenance sites still quote
  `…,'doc-sync','human']`, `orky-action-dispatch.md` still claims expected `` `1` ``, and the
  `[Unreleased]` provenance record is absent.
- `tests/e2e/orky-pane-resolution.spec.ts` (TEST-716/TEST-717) compiles under `tsconfig.e2e.json`
  (typecheck green) but was NOT executed in this phase — e2e runs against a built `out/` and is
  outside the `npm test` gate; against the shipped OrkyPane it is RED by construction (no finding
  row renders any `— resolution:` affix). It runs at the implement/review verification alongside
  the rest of the Playwright suite (`npm run build && npm run e2e`).
- `tests/integration/orky-act-loop.test.ts` TEST-691 — already RED on a plugin-0.30.0 machine
  (live `contract_version` 2 vs the pin 1); NOT modified in this phase, per REQ-108.

## Frozen-test edits sanctioned by this phase (the spec's baseline note)

- `tests/main/orky-cli-contract-handshake.test.ts`: `goodContract` baseline 1→2 (TASK-102);
  "v2 → mismatch" INVERTED to TEST-708's v1 case; version literals inside `goodContract` overrides
  adjusted in place (`contractVersion` expectation 1→2 in the inserted-phase case; the combined
  drift case feeds 1 instead of 2; the cached-mismatch case keeps 3). No other assertion changed.
- `tests/shared/orky-contract-golden.test.ts`: additive describe-block only (TEST-711/TEST-712);
  every pre-existing assertion byte-for-byte unchanged.
- NOT touched: `tests/integration/orky-act-loop.test.ts` (TEST-691/TEST-702), every 0004..0014
  frozen suite, `tests/fixtures/orky-contract/*` (regeneration is TASK-103, **tests phase /
  test-designer actor** — via the generator ONLY, moved from implement by the first loopback
  amendment; the original "TASK-103, implementer" phrasing here was an artifact of an earlier draft
  and is *superseded* by the amendment banner at the top of this file and the "TASK-103 execution
  record" section below, both of which correctly record the tests-phase/test-designer actor and the
  0.32.0/`1c59d74` producer — FINDING-009), `ORKY_PHASES`, all `src/`.

## Notes for the implementer

- The fixtures turning TEST-711 green MUST come from one run of
  `node tools/generate-orky-contract-fixtures.mjs` against the installed v2-contract plugin — a
  hand-edited fixture violates binding decision 3 even if it makes the test pass.
- TEST-715 allows at most ONE strict `status === 'resolved'` in `OrkyPane.tsx` — that budget is the
  EXISTING escalation-row check; write the finding-row check case-folded.
- TEST-717 pins the open / bare-resolved rows' EXACT text — add nothing (not even trailing
  whitespace-visible artifacts) to non-affixed rows. TEST-724 extends the same discipline to the
  empty-string vector.
- TEST-716 pins the formatted instant `2026-07-01 00:00:00 UTC` — `formatOrkyInstant` output for
  the seeded epoch; surface it in the row text or the row's `title`, either satisfies. TEST-723
  (descent) strengthens this: whenever the affix renders, the TITLE must carry claim + resolution
  details (TASK-120's title-mirror MUST).
- (Descent) TEST-722 pins the exact non-empty guard shape the amended spec names
  (`resolution !== null && resolution !== ''`) and requires the finding row's `title` to stop being
  the unconditional bare claim — compose the affix once and feed both the row and the title
  (TASK-120); non-affixed rows keep the bare-claim semantics.
- (Descent) TEST-720 goes green when CLAUDE.md:179's "(intake…human)" is fixed (TASK-118);
  TEST-719's CHANGELOG half when the provenance is reworded to
  `regenerated against plugin 0.32.0 (commit \`1c59d74\`)`-shape with the generation-time producer
  leading (TASK-119); TEST-721 when `docs/features/orky-pane.md` gains the resolution-display
  section (TASK-121).

## TASK-103 execution record (tests-phase re-entry, per amended 03-plan.md)

- 2026-07-03: fixtures regenerated by ONE run of
  `ORKY_PLUGIN_DIR=C:\dev\Orky\plugin node tools/generate-orky-contract-fixtures.mjs`
  against the installed Orky plugin **0.32.0** (`C:/dev/Orky/plugin`, commit `1c59d74`) — plugin
  had moved past the 0.30.0 named at spec time; the emitted contract is still `contract_version: 2`
  with the same v2 shape, so all TEST-711/TEST-712 pins hold unchanged. No hand-edits. Verified:
  `contract_version === 2`, 8-entry `phases` ≡ `ORKY_PHASES`, `phase_order` `intake`→`human-review`,
  `finding_resolution` + `producer_tiers` present, `state.gate_fields` contains `firstAt`.
  Golden suite GREEN unmodified (8/8); feature suite still RED: handshake 7f/10p,
  root-detail-resolution 2f/0p, pane-resolution-structure 1f/0p, docs-0015 4f/0p,
  v2-blocking 0f/1p (green by design).

## Tests-phase loopback #2 record (2026-07-03)

Cross-feature frozen-guard collision, surfaced by the implement-phase self-verify: TEST-645
(`tests/docs-feature-0010-loopback.test.ts`, feature 0010's inventory sweep, pattern
`/orky-pane-row-actions|OrkyEntryActions|openOrkyCapture|orky-entry-actions|F10/i`) failed because
`tests/renderer/orky-pane-resolution-structure.test.ts:15` said "the shipped F9/F10 OrkyPane".
Disposition: reworded to "the shipped 0009/0010-era OrkyPane" (comment-only; no assertion, import,
or vector changed; TEST-715's behavior identical). Chosen over extending TEST-645's dispositioned
list because the 0015 file's pattern hit was incidental prose, not a real consumer of the 0010
surface. Verified post-edit: TEST-645 green, TEST-715 still RED, zero pattern hits across all
seven 0015 test files.

## ESC-001 descent — tests-phase re-entry record (2026-07-03, TASK-115/116/117)

Targeted amendment of exactly the three plan-named files (no other test redesigned, no fixture
touched, no `src/` change, no `.orky/` control-file write):

- `tests/docs-feature-0015.test.ts` — **TASK-115:** TEST-719's CHANGELOG `it` re-pinned: the
  `toContain('0.30.0')` requirement is gone; the assertion now requires the version-agnostic shape
  `/regenerated against (?:the )?(?:installed )?plugin\s+\*{0,2}v?\d+\.\d+\.\d+\*{0,2}\s+\(commit\s+`?[0-9a-f]{7,40}`?\)/i`
  plus the auditable generator command — the generation-time recorded producer (0.32.0 /
  `1c59d74`) satisfies it, and so does every future re-mirror without a test edit (the plan's
  preferred encoding; no version numeral is required or forbidden). **TASK-117:** TEST-720 added
  (REQ-107 scan (4), frozen: `/intake.{1,5}human/` over CLAUDE.md + the two provenance sites, every
  hit must continue `-review` or be explicitly historical within ±200 chars) and TEST-721 added
  (REQ-113 doc pins over `docs/features/orky-pane.md` — the acceptance's "docs test" option).
- `tests/renderer/orky-pane-resolution-structure.test.ts` — **TASK-116 (structural half):**
  TEST-722 added; TEST-715 untouched (assertion-identical).
- `tests/e2e/orky-pane-resolution.spec.ts` — **TASK-116 (rendered half):** TEST-723 (title mirror)
  and TEST-724 (empty-string guard) added; the shared seed gained the F-EMPTY vector and
  `openExpandedPane`'s finding count moved 4→5 (mechanical — TEST-716/TEST-717's assertion bodies
  are unchanged and not weakened).

### Descent RED evidence (2026-07-03, against the post-first-pass working tree)

`npm run typecheck` fully green (all three tsconfigs — the e2e additions compile under
`tsconfig.e2e.json`). `npx vitest run tests/docs-feature-0015.test.ts
tests/renderer/orky-pane-resolution-structure.test.ts`: **4 failed / 4 passed** — every failure is
a descent-amended assertion, everything pre-existing in those files stays green:

- TEST-719 (CHANGELOG `it`) RED: the current `[Unreleased]` entry reads "…regenerated in one run
  of `node tools/generate-orky-contract-fixtures.mjs`, targeting the spec's named plugin
  **0.30.0**…" — no `regenerated against plugin <semver> (commit <sha>)` shape, no commit sha at
  all (unfixed until TASK-119). The file's other three `it`s (orky-status.ts / orky-status.md /
  orky-action-dispatch.md, reconciled by the first pass) remain GREEN.
- TEST-720 RED: `CLAUDE.md:179 — prose mirror "intake…human" … must continue "-review" or be
  explicitly labeled historical/pre-v2` (the FINDING-005/012 site, unfixed until TASK-118). The
  two provenance sites contribute zero disqualifying hits (already reconciled).
- TEST-721 RED: `docs/features/orky-pane.md` contains none of the three field names and no
  `— resolution:` passage (section unwritten until TASK-121).
- TEST-722 RED: `OrkyPane.tsx`'s guard is still only `resolution !== null` (no
  `/resolution\s*!==\s*(''|"")/` match) — the open-tag `title={fd.claim}` half is also still
  present (unfixed until TASK-120). TEST-715 remains GREEN (first-pass TASK-112 code).
- TEST-723/TEST-724 (e2e): compile-verified only, per this file's standing e2e convention — RED by
  construction against the working tree: the finding row's `title` is the bare `fd.claim` (no
  mirror → TEST-723 fails) and the `!== null`-only guard renders F-EMPTY's dangling
  `— resolution:` label plus the orphaned `(kevin)`/`at 2026-07-01 00:00:00 UTC` parts
  (→ TEST-724's `toHaveText`/`not.toContain` pins fail). Executed at the implement/review
  verification (`npm run build && npm run e2e`).

Cross-feature guard re-checked: TEST-645 (`tests/docs-feature-0010-loopback.test.ts`) GREEN against
the amended files. Freeze snapshot to be re-captured over these edits per the amended plan.
