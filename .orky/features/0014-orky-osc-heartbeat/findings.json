[
  {
    "id": "FINDING-001",
    "lens": "doc-drift",
    "severity": "LOW",
    "gate": "review",
    "location": ".orky/features/0014-orky-osc-heartbeat/traceability.json + traceability.md",
    "claim": "Filed against the prior (8888/key=value) iteration's traceability matrix, which pointed at src/main/services.ts and src/main/ipc/register-orky.ts (untouched) instead of the real composition root src/main/ipc/register.ts, and missed src/shared/types.ts as OrkyHeartbeat's home.",
    "fix": "N/A — superseded by the full plan/tests rewrite this cycle.",
    "phase_of_origin": "implement",
    "status": "resolved",
    "verified_by": "doc-drift (re-verified 2026-06-30 against the rewritten traceability.json/.md, which now correctly lists src/main/ipc/register.ts as the composition root and src/shared/types.ts as OrkyHeartbeat's home; FINDING-001's specific drift has not recurred)",
    "contract_violation": false
  },
  {
    "id": "FINDING-CODEX-001",
    "lens": "codex-cross-check",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts (OLD, now-replaced key=value decodeHeartbeat)",
    "claim": "Filed against the prior (8888/key=value) iteration's body decode: the 256-char cap was measured with body.length (UTF-16 units) after splitting into pairs, so an unknown forward-compat pair could carry non-ASCII/control bytes past a byte-accurate cap.",
    "fix": "Carried forward as binding REQ-007 in the rewritten 02-spec.md: cap must be measured as actual UTF-8 byte length, before JSON.parse.",
    "phase_of_origin": "implement",
    "contract_violation": true,
    "status": "resolved",
    "verified_by": "security (2026-06-30): REQ-007 is correctly implemented in the new code at src/main/status/orky-osc-parser.ts:75-79 — `Buffer.byteLength(payload, 'utf8')` checked strictly before `JSON.parse`. TEST-016 (emoji-padded reason, .length under cap but UTF-8 bytes over) confirms rejection. The old key=value grammar this finding was filed against no longer exists. RE-VERIFIED security (loopback iter3, 2026-06-30): this iteration's diff is scoped entirely to src/shared/orky-status.ts's heartbeatKind() (REQ-009 kind-derivation, FINDING-DA-004/ESC-002) plus test/fixture/spec files; orky-osc-parser.ts (decodeHeartbeat, lines 75-79) is byte-for-byte unchanged in `git show HEAD`. Re-ran tests/main/orky-osc-parser.test.ts (20/20 green, incl. TEST-016) against the current worktree — still correctly resolved, no regression introduced by this iteration."
  },
  {
    "id": "FINDING-SEC-001",
    "lens": "security",
    "severity": "HIGH",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts (OLD, now-replaced parser) — root cause in src/main/status/osc-scanner.ts (scanOsc, shared/pre-existing)",
    "claim": "Filed against the prior iteration: scanOsc's no-terminator branch returns the entire accumulated buffer, not a bounded carry-over, so a 12-byte prefix followed by sustained non-terminating PTY output caused unbounded heap growth in the privileged Electron main process.",
    "fix": "Carried forward as binding REQ-006 in the rewritten 02-spec.md: explicit MAX_PENDING_BYTES ceiling enforced independently of scanOsc's carry-over.",
    "phase_of_origin": "spec",
    "contract_violation": true,
    "status": "resolved",
    "verified_by": "security (2026-06-30): REQ-006 is correctly implemented in the new code at src/main/status/orky-osc-parser.ts:111-126 — `MAX_PENDING_BYTES = 8192` enforced after every push, independent of scanOsc, margin verified correct (7 + 4096 + 2 = 4105 < 8192, so no in-flight legal marker can be dropped). TEST-014 pushes 6 MiB across 96 chunks and asserts the internal buffer stays bounded (not just no-throw). devils-advocate review (2026-06-30) independently confirmed this arithmetic. RE-VERIFIED security (loopback iter3, 2026-06-30): this iteration's diff (`git show HEAD`) touches only src/shared/orky-status.ts's heartbeatKind() — orky-osc-parser.ts's push()/MAX_PENDING_BYTES guard (lines 111-126) is byte-for-byte unchanged. Re-ran tests/main/orky-osc-parser.test.ts (20/20 green, incl. TEST-014 asserting the buffer stays ≤ MAX_PENDING_BYTES) against the current worktree — still correctly resolved, no regression introduced by this iteration."
  },
  {
    "id": "FINDING-DET-001",
    "lens": "determinism",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "src/main/orky/orky-stream-status.ts (OrkyStreamStatusBridge) / tests",
    "claim": "The stateful fs/stream combiner OrkyStreamStatusBridge -- the ONLY event-arrival-order-sensitive component in this feature and the actual production applier of REQ-013's filesystem-wins precedence -- has no behavioral test. Only the pure selectOrkyPaneStatus precedence rule is unit-tested; the bridge that holds per-pane Map state, applies precedence under interleaved fs/stream arrival, dedups via JSON.stringify, and clears on pane teardown is exercised by nothing.",
    "fix": "Add a behavioral unit test for OrkyStreamStatusBridge asserting order-independent convergence: drive setFsStatus-then-setStreamHeartbeat and setStreamHeartbeat-then-setFsStatus for the same pane id and assert the final emitted status is identical (filesystem wins) regardless of arrival order; assert an unchanged recompute is deduped (no duplicate emit); assert clearPane drops the pane so a later setStreamHeartbeat starts clean.",
    "phase_of_origin": "tests",
    "status": "open",
    "verified_by": "determinism (re-verified 2026-06-30): src/main/orky/orky-stream-status.ts was NOT touched by this implementation pass (consumes the parser's output type, not its wire format) and still has no behavioral test. Code remains order-convergent and correct today, but a future regression (swapped selectOrkyPaneStatus args, dropped lastKey dedup, unioned fs+stream) would pass the whole suite undetected.",
    "contract_violation": false
  },
  {
    "id": "FINDING-QUAL-001",
    "lens": "quality",
    "severity": "LOW",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts (OLD, now-replaced key=value decodeHeartbeat)",
    "claim": "Filed against the prior iteration's key=value field-collection loop: a repeated key silently kept only the last occurrence, undocumented and untested.",
    "fix": "N/A — the body grammar is now JSON, not key=value pairs.",
    "phase_of_origin": "implement",
    "contract_violation": false,
    "status": "not-applicable",
    "verified_by": "quality (2026-06-30): the new decodeHeartbeat calls JSON.parse once and reads only seven named fields off the result; duplicate-key resolution is now governed by ECMA-262's well-defined JSON.parse semantics (last-occurrence-wins is standard language behavior), not a hand-rolled merge loop. No analogous issue exists in the new code."
  },
  {
    "id": "FINDING-DA-001",
    "lens": "devils-advocate",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts:92-100 (decodeHeartbeat, REQ-004 verbatim mapping) + src/shared/orky-status.ts:364-375 (heartbeatDetail)",
    "claim": "A well-formed heartbeat marker remains fundamentally UNAUTHENTICATED. Any process that can write to the PTY can fabricate a status for an arbitrary feature slug — including a fake needsHuman:true to draw operator attention to a non-event, or a fake low-urgency state to mask a real one. REQ-004 explicitly mandates verbatim trust (Termhalla is a thin client, never 'corrects' Orky's verdict) — by design, not a bug, but the trust boundary is not documented anywhere. Additionally, `reason` is free-form attacker prose rendered verbatim into Termhalla's chrome via heartbeatDetail, and a spoofed needsHuman:true rides the same orky:status channel that drives border/tab-badge/notification alerts in 0004's presentation — not 'purely cosmetic' in Termhalla even though ADR-026 frames it that way on the Orky side.",
    "fix": "State the trust boundary explicitly in docs/features/orky-osc-heartbeat.md (stream-derived status is only as trustworthy as whatever writes to the PTY) and visually distinguish stream-derived (untrusted) status from filesystem-derived (authoritative) status so a spoofed bare-SSH heartbeat is not presented identically to a verified local one.",
    "phase_of_origin": "spec",
    "status": "open",
    "verified_by": "devils-advocate (re-verified 2026-06-30 against the new ADR-026/JSON code — concern persists, relocated to current file:line)",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-002",
    "lens": "devils-advocate",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "src/main/orky/orky-stream-status.ts:39-48 (recompute) + src/main/orky/orky-tracker.ts:64-99 (async watch/unwatch) / REQ-013",
    "claim": "REQ-013's 'filesystem wins whenever present' guarantee holds only spatially (a null check in selectOrkyPaneStatus), not temporally. OrkyTracker.watch() is async (awaits findOrkyRoot then reread before its first fs emit) and unwatch() emits fs=null for a still-live pane. For a LOCAL pane that DOES have a .orky/ tree, a heartbeat decoded from the stream during the fs-null window (startup before first reread, or after a mid-life unwatch) makes the bridge emit a lower-trust stream-derived status on a pane 0004 is supposed to own exclusively. Latent today only because no real Orky-side emitter existed yet at the time of the original finding; now that ADR-026 emission is real and opt-in, it activates the moment a local Orky pane with config.heartbeat.osc enabled streams a marker during one of these windows.",
    "fix": "Make the bridge suppress stream-derived status for any pane known to be (or to have ever been) filesystem-watched, rather than relying on fs being momentarily non-null: track per-pane whether an orky:watch is active and prefer last-known fs ownership through transient fs-null emits.",
    "phase_of_origin": "plan",
    "status": "open",
    "verified_by": "devils-advocate (re-verified 2026-06-30 — orky-stream-status.ts/orky-tracker.ts unchanged by this pass, concern persists)",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-003",
    "lens": "devils-advocate",
    "severity": "LOW",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts (OLD parser's `v !== '1'` reject-whole)",
    "claim": "Filed against the prior iteration: rejecting the whole marker on an unrecognized version was in tension with the forward-compat unknown-key-ignore policy, risking a silent total blackout on an additive Orky-side version bump.",
    "fix": "REQ-005 in the rewritten spec explicitly reverses this: forward-compatible, not fail-closed, on version + unknown fields.",
    "phase_of_origin": "spec",
    "status": "resolved",
    "verified_by": "devils-advocate (2026-06-30): decodeHeartbeat does no `v` gating at all (src/main/status/orky-osc-parser.ts:75-101) — v:2, missing v, and unknown fields all decode (TEST-011/012/013). The blackout risk is genuinely fixed (residual risk of silent misrender on a breaking v bump tracked separately as FINDING-DA-006, LOW, non-blocking).",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-004",
    "lens": "devils-advocate",
    "severity": "HIGH",
    "gate": "review",
    "location": "src/shared/orky-status.ts:352-356 (heartbeatKind) vs. C:/dev/Orky/plugin/gatekeeper/gatekeeper.js:887-910 (oscHeartbeat, the real emitter) and CLAUDE.md's 'Orky status reads active.json.phase + gate topology — NEVER state.json.phase' gotcha",
    "claim": "heartbeatKind derives 'done' only when `hb.phase == null && hb.gateN >= hb.gateM`. Independently verified against the REAL Orky emitter (C:/dev/Orky/plugin/gatekeeper/gatekeeper.js:904): `phase: state.phase || d.phase || null`. Per this repo's own CLAUDE.md gotcha, a fully complete feature's state.json.phase is the string 'doc-sync' (truthy — human-review is tracked as a separate external gate, never written into state.json.phase), so the REAL emitter sends `phase:\"doc-sync\", gate:\"8/8\", needsHuman:false` for a genuinely complete feature — never `phase: null`. heartbeatKind's done-branch can therefore NEVER fire for a real completed feature; the chip renders 'busy' forever for any feature that finishes its pipeline while a bare-SSH pane is streaming its heartbeat. This defeats a core scenario the feature exists for (accurate status, including completion, with zero filesystem access). The frozen 02-spec.md's own REQ-009 acceptance criteria only exercise a synthetic `phase: null` complete-heartbeat fixture that likely never occurs on the real wire per the real emitter's logic verified above — i.e. the contradiction is baked into the FROZEN SPEC itself (REQ-009's derivation rule vs. ADR-026's actual `state.phase || d.phase || null` formula), not just an implementation bug; the code correctly implements what the spec says, but what the spec says does not match the real wire.",
    "fix": "Re-derive 'done' from gate fullness alone (`gateN >= gateM`) regardless of phase value, mirroring 0004's gate-based isComplete (`f.gates['human-review']?.passed === true`) rather than 0004's phase-null gateFrontier convention (which is a LOCAL Termhalla computation, not what the wire actually carries). Requires a REQ-009 spec correction (loopback to spec) plus a corrected test fixture reflecting the real emitter's `phase:\"doc-sync\", gate:\"8/8\"` shape for a complete feature, not the literal-null fixture currently frozen.",
    "phase_of_origin": "spec",
    "status": "resolved",
    "verified_by": "devils-advocate (loopback iter3, 2026-06-30): GENUINELY RESOLVED, confirmed by an independent re-derivation (not merely 'tests pass'). Re-derived against the real-emitter ground truth — a complete feature ALWAYS has gateN >= gateM regardless of the accompanying phase string, and an incomplete feature NEVER has gateN >= gateM. heartbeatKind (src/shared/orky-status.ts:358-362) now reads `if (hb.needsHuman === true) return 'needs-input'; if (hb.gateN >= hb.gateM) return 'done'; return 'busy'` — the `hb.phase == null &&` clause is gone, so done-detection is purely gate-based and reads `phase` nowhere (grep for `phase == null`/`phase === null` across orky-status.ts: zero matches). The frozen-SPEC contradiction this finding flagged is also gone: REQ-009 was rewritten to mandate gate-based, phase-independent done-detection, and its acceptance now carries the REAL emitter's complete shape `{phase:\"doc-sync\",gate:\"8/8\",needsHuman:false}` as the PRIMARY done example (with `phase:null,gate:\"8/8\"` retained only as a phase-independence companion); TASK-005 (03-plan.md) corrected to match. TEST-040 is a genuine regression guard, not a tautology: it feeds DONE_DOC_SYNC_HEARTBEAT (the real `phase:\"doc-sync\"` shape) and asserts the clean-done/idle roll-up — under the OLD `phase==null && gateN>=gateM` rule it returned 'busy' (because \"doc-sync\" !== null short-circuits the AND), so it fails RED on the old code and passes ONLY on the corrected gate-based rule. Ran `npx vitest run tests/shared/orky-heartbeat-status.test.ts`: 12/12 green incl. TEST-040/025/041. The 'busy forever for a completed bare-SSH feature' defect is fixed — a clean-done heartbeat now rolls up to the cleared/idle shape exactly as 0004 does for a completed filesystem feature (clean-done excluded from the popover). Residual: a phase-null label nit on the stream path (a busy/incomplete heartbeat whose phase is null still labels itself 'done' via the retained `?? 'done'` fallback) is tracked separately as FINDING-DA-009 (LOW, non-blocking) — it does NOT reopen DA-004's done-detection defect, which is correctly resolved.",
    "contract_violation": true
  },
  {
    "id": "FINDING-DA-005",
    "lens": "devils-advocate",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "src/shared/orky-status.ts:386-401 (heartbeatToFeatureStatus, lastActivityAt hard-coded to 0) + src/main/orky/orky-stream-status.ts (no time input)",
    "claim": "The whole premise is a heartbeat (liveness), yet the stream path discards timing entirely: lastActivityAt is hard-coded to 0 and nothing in the parser/bridge uses arrival time. After the remote Orky process exits or the SSH session goes idle — the exact bare-SSH scenario this feature targets — the last needs-human/busy heartbeat stays pinned indefinitely, indistinguishable from a live run. The filesystem path has stall detection (STALL_THRESHOLD_MS/isStalled); the stream path has no equivalent. setStreamHeartbeat(id, null)'s documented clear path is dead code in production (nothing currently calls it with null outside teardown).",
    "fix": "Stamp each heartbeat with arrival time and expire/grey stale stream status past a threshold (reuse STALL_THRESHOLD_MS), or clear the stream slot on PTY command-done/idle. Add a REQ + test for stream-status staleness.",
    "phase_of_origin": "spec",
    "status": "open",
    "verified_by": "devils-advocate",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-006",
    "lens": "devils-advocate",
    "severity": "LOW",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts:75-100 (decodeHeartbeat) / REQ-005",
    "claim": "REQ-005's forward-compat reversal (FINDING-DA-003) fixed the blackout risk but introduces an unexamined residual: a future v:2 that REDEFINES an existing field's meaning (not just adds one) would be silently decoded with v1 semantics and render WRONG status, not blank status — undetectable cross-repo since the parser never fails closed on version.",
    "fix": "Document the residual misrender risk in the REQ-016 cross-repo drift caveat; optionally log-once (not reject) on an unrecognized v for diagnosability.",
    "phase_of_origin": "spec",
    "status": "open",
    "verified_by": "devils-advocate",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-007-OSC",
    "lens": "devils-advocate",
    "severity": "LOW",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts:93 (decodeHeartbeat feature field) + src/shared/orky-status.ts:389,408",
    "claim": "decodeHeartbeat maps an empty-string `feature` (`{\"feature\":\"\"}`) to `feature: \"\"`, not `null`. orkyHeartbeatToPaneStatus's `hb.feature === null` check is then false (not treated as app-loop/cleared), and heartbeatToFeatureStatus's `hb.feature ?? '(unknown)'` does not catch empty string (only null/undefined) — a chip renders with a blank name. Named -OSC to avoid collision with the existing FINDING-DA-007 (0004's null-phase chip guard, a different feature).",
    "fix": "Treat empty/whitespace-only feature as null (app-loop) in decodeHeartbeat, or use a truthiness check (`hb.feature || '(unknown)'`) in the mapper.",
    "phase_of_origin": "implement",
    "status": "open",
    "verified_by": "devils-advocate",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-008",
    "lens": "devils-advocate",
    "severity": "LOW",
    "gate": "review",
    "location": "src/main/orky/orky-stream-status.ts:9-14, src/shared/orky-status.ts:412",
    "claim": "TASK-007 left orky-stream-status.ts unedited (regression-checked only), so its comments still cite the OLD 13-REQ numbering ('REQ-009' for filesystem-wins, 'REQ-008' for no-new-channel, 'REQ-013' for no-persistence) which under the rewritten 17-REQ spec are actually REQ-013/REQ-012/REQ-017 respectively. selectOrkyPaneStatus's header in orky-status.ts:412 likewise cites the stale '(REQ-009)'.",
    "fix": "Update the REQ-id citations in the unchanged bridge/selector comments to the current frozen numbering.",
    "phase_of_origin": "implement",
    "status": "open",
    "verified_by": "devils-advocate",
    "contract_violation": false
  },
  {
    "id": "FINDING-DET-002",
    "lens": "determinism",
    "severity": "LOW",
    "gate": "review",
    "location": "src/main/status/orky-osc-parser.ts:111-126 (MAX_PENDING_BYTES reset) interacting with src/main/status/osc-scanner.ts (scanOsc swallow-until-terminator)",
    "claim": "The REQ-006 bounded-ceiling reset makes emission of a LATER valid marker depend on PTY chunk boundaries: for an unterminated-prefix flood exceeding MAX_PENDING_BYTES followed by a later valid marker, whether that later marker parses or is lost (swallowed into one over-long body by scanOsc) depends on exactly when the reset fires relative to chunk delivery. Same on-the-wire bytes can light a status chip on one run/host and not another. Bounded effect (read-only cosmetic status, adversarial input class), hence LOW.",
    "fix": "Reconcile the REQ-006 reset with scanOsc's swallow semantics — e.g. retain only a trailing ORKY_OSC-prefix-sized window on reset rather than dropping to empty — and add a behavioral test feeding a flood-then-valid-marker stream at multiple chunk granularities.",
    "phase_of_origin": "spec",
    "status": "open",
    "verified_by": "determinism",
    "contract_violation": false
  },
  {
    "id": "FINDING-DD-001",
    "lens": "doc-drift",
    "severity": "MEDIUM",
    "gate": "review",
    "location": ".orky/features/0014-orky-osc-heartbeat/state.json (traceability block)",
    "claim": "state.json's traceability block is stale relative to the rewritten suite and canonical traceability.json/04-tests.md: records testIds TEST-001..TEST-042 (42 entries; the current suite is now TEST-001..TEST-041 = 41 after this iteration's ESC-002 revision added TEST-040/TEST-041 — so the list's only remaining phantom is TEST-042, narrowed from the three phantoms 040/041/042 it carried when the suite was 39), lists reqsWithoutUnitTests as [REQ-011, REQ-012] (wrong — those ARE unit-tested per traceability.json: REQ-011→TEST-029/030, REQ-012→TEST-031; the genuinely doc-only REQs are REQ-015/REQ-016), and cites a phantom TASK-010 in reqsWithoutUnitTestsReason (the plan has TASK-001..TASK-008 only).",
    "fix": "Refresh state.json's traceability block to testIds TEST-001..TEST-041, reqsWithoutUnitTests [REQ-015, REQ-016], reason citing TASK-008. (Editing state.json is the orchestrator's job, not the reviewer's — this finding only records the discrepancy.)",
    "phase_of_origin": "tests",
    "status": "open",
    "verified_by": "doc-drift (re-verified 2026-06-30, loopback iter3 against state.json lines 126-182): the traceability block still lists testIds through TEST-042 and reqsWithoutUnitTests [REQ-011, REQ-012] with the TASK-010 reason. This iteration grew the real suite from 39 to 41 (added TEST-040/041), narrowing but NOT closing the discrepancy — TEST-042 remains phantom, and the wrong reqsWithoutUnitTests list + phantom-TASK-010 reason are unchanged.",
    "contract_violation": false
  },
  {
    "id": "FINDING-DD-002",
    "lens": "doc-drift",
    "severity": "LOW",
    "gate": "review",
    "location": "tests/main/orky-osc-parser.test.ts:22-23, tests/main/orky-osc-structural.test.ts:14-16, tests/shared/orky-heartbeat-status.test.ts:17-20 (header comments)",
    "claim": "The three frozen test files carry present-tense TDD RED-phase header comments (e.g. 'Runs RED: ... still implements the superseded 8888/key=value contract'). Post-implement these are false present-tense claims (the suite is GREEN; the parser implements the 9999/JSON contract).",
    "fix": "At doc-sync (or next sanctioned touch), reword to past tense scoped to the tests gate ('Was RED at the tests gate because...').",
    "phase_of_origin": "tests",
    "status": "open",
    "verified_by": "doc-drift (re-verified 2026-06-30, loopback iter3): all three 'Runs RED:' present-tense header comments are still false post-implement (suite is GREEN; the parser implements the 9999/JSON contract). This iteration touched tests/shared/orky-heartbeat-status.test.ts (added an ITERATION NOTE at lines 22-28, expanded the fixture imports, and added the TEST-040/TEST-041 bodies) but did NOT reword its 'Runs RED:' comment, which remains at lines 17-20 and still reads in the present tense; tests/main/orky-osc-parser.test.ts:22 and tests/main/orky-osc-structural.test.ts:14 were untouched this iteration and their comments are likewise unchanged. All cited line numbers re-confirmed accurate.",
    "contract_violation": false
  },
  {
    "id": "FINDING-QUAL-002",
    "lens": "quality",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "src/shared/orky-status.ts:397",
    "claim": "`phase: hb.phase as OrkyPhase | null` force-casts OrkyHeartbeat.phase (typed OrkyPhase | string | null specifically because REQ-004 mandates verbatim passthrough of arbitrary strings) into OrkyFeatureStatus.phase's narrower closed union. Currently harmless (the value is only ever stringified, never switched on), but misrepresents the type system's guarantees for any future code that pattern-matches on it.",
    "fix": "Widen OrkyFeatureStatus.phase to OrkyPhase | string | null (documenting the heartbeat path's wider range), or introduce a small honest local type instead of the `as` cast.",
    "phase_of_origin": "implement",
    "status": "open",
    "verified_by": "quality (re-verified 2026-06-30, loopback iter3: location drifted 391->397 — iteration 3's 6-line REQ-009 doc-comment expansion above heartbeatKind pushed every later line in the file down by 6; `git show HEAD` confirms heartbeatToFeatureStatus's cast itself is untouched by this iteration, claim still holds verbatim)",
    "contract_violation": false
  },
  {
    "id": "FINDING-QUAL-003",
    "lens": "quality",
    "severity": "LOW",
    "gate": "review",
    "location": "src/shared/orky-status.ts:295 (chipLabel) and :380 (heartbeatDetail)",
    "claim": "Both independently encode the same 'null phase renders as the literal done' convention as `?? 'done'`, in the same file, not centralized.",
    "fix": "Extract a tiny shared helper, e.g. `const phaseLabel = (p) => p ?? 'done'`, used at both sites.",
    "phase_of_origin": "implement",
    "status": "open",
    "verified_by": "quality (re-verified 2026-06-30, loopback iter3: chipLabel's line is unchanged at :295 (it precedes this iteration's edit point in the file); heartbeatDetail's line drifted 374->380 for the same 6-line-shift reason as FINDING-QUAL-002. Both `?? 'done'` call sites are otherwise byte-for-byte untouched by this iteration; claim still holds verbatim)",
    "contract_violation": false
  },
  {
    "id": "FINDING-QUAL-005",
    "lens": "quality",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "src/shared/types.ts:71 (OrkyHeartbeat.phase field doc-comment)",
    "claim": "This iteration (ESC-002/FINDING-DA-004) corrected the misleading 'a null phase implies done' framing everywhere it touched: src/shared/orky-status.ts's file-level wire-shape comment, heartbeatKind()'s JSDoc, and 02-spec.md's reproduced interface excerpt (02-spec.md line 147) all now explicitly distinguish 'null only falls back to the chip LABEL' from 'kind is derived purely from gates, never phase'. But the iteration never touched src/shared/types.ts, which defines the ACTUAL canonical `OrkyHeartbeat` interface that `heartbeatKind(hb: OrkyHeartbeat)` (src/shared/orky-status.ts:358) imports and operates on (`import type { ... OrkyHeartbeat } from './types'`, orky-status.ts:23). Its inline field comment at types.ts:71 still reads verbatim from before this fix: `phase: OrkyPhase | string | null // payload.phase, verbatim; null -> rendered \"done\"` — the same unqualified 'null -> done' phrasing whose ambiguity (does 'rendered done' mean the LABEL or the `kind`?) is the exact class of misreading this whole iteration exists to correct (the analogous, now-corrected REQ-009 spec wording produced FINDING-DA-004). A future maintainer reading the type's own doc-comment — the single most likely place to consult this field's contract — gets no indication that `kind` is gate-only and may reintroduce a `phase` check into `heartbeatKind`, regressing the exact bug this iteration just fixed.",
    "fix": "Update src/shared/types.ts:71's inline comment to the corrected wording already used elsewhere in this iteration's diff, e.g. `phase: OrkyPhase | string | null // payload.phase, verbatim; null only affects the chip LABEL fallback ('done'), never the kind derivation (gate-based only, see heartbeatKind / REQ-009)`.",
    "phase_of_origin": "implement",
    "status": "resolved",
    "verified_by": "quality (2026-06-30, loopback iter3 review): confirmed via `git show HEAD` that src/shared/types.ts is not in this commit's changed-file list, and via direct read that types.ts:71 still carries the pre-iteration-3 comment while orky-status.ts:341-346/352-357 and 02-spec.md:147 were all updated to the corrected, disambiguated wording. RESOLVED doc-sync (2026-06-30): src/shared/types.ts:71's `OrkyHeartbeat.phase` inline comment rewritten to `payload.phase, verbatim; null only affects the chip LABEL fallback ('done'), never the kind derivation (gate-based only, see heartbeatKind / REQ-009)` — matches the corrected wording used elsewhere in this iteration's diff (orky-status.ts, 02-spec.md). Comment-only change; no logic touched.",
    "contract_violation": false
  },
  {
    "id": "FINDING-QUAL-004",
    "lens": "doc-drift",
    "severity": "HIGH",
    "gate": "doc-sync",
    "location": "docs/features/orky-osc-heartbeat.md (entire OSC marker CONTRACT section), CHANGELOG.md [Unreleased]",
    "claim": "Both files still describe the superseded 8888/key=value contract verbatim, not the real shipped ADR-026 (9999/JSON) contract. This is REQ-016's binding artifact. Quality and security lenses both flagged this independently; doc-drift lens confirmed via the spec's own Definition of Done that this is EXPECTED, NOT review-gate-blocking scope explicitly deferred to the doc-sync phase (implementer's report and 02-spec.md's DoD both state REQ-015/REQ-016 doc deliverables are 'verified at the doc-sync gate'). Re-scoped from 'review' to 'doc-sync' gate accordingly so it does not spuriously block the review gate; doc-sync MUST close this before the feature can be considered done.",
    "fix": "doc-sync phase: rewrite docs/features/orky-osc-heartbeat.md's contract section + worked example to the real \\x1b]9999;<json>\\x07 ADR-026 contract (copy from 02-spec.md's 'OSC marker CONTRACT' section verbatim) and update CHANGELOG.md [Unreleased] to match (wire format + cap numbers). Also fold in FINDING-DA-001's trust-boundary caveat while rewriting.",
    "phase_of_origin": "implement",
    "status": "resolved",
    "verified_by": "quality, security, doc-drift (consolidated 2026-06-30 — gate corrected from review to doc-sync per the spec's Definition of Done, which explicitly routes REQ-015/REQ-016 verification there). RESOLVED doc-sync (2026-06-30): docs/features/orky-osc-heartbeat.md's OSC marker CONTRACT section fully rewritten to the real ADR-026 contract (introducer \\x1b], code 9999, single first-';'-only separator, single-line compact JSON payload with the 7-field v/feature/phase/gate/needsHuman/reason/action schema table, BEL terminator MUST-accept/ST MAY-accept), citing src/main/status/orky-osc-parser.ts (ORKY_OSC/decodeHeartbeat) as the in-repo source and orky-adr-026-osc-heartbeat.md as the upstream source; both worked examples (app-loop tick, awaiting-human feature) reproduced as complete on-the-wire byte strings. Added a new 'Trust boundary' subsection folding in FINDING-DA-001's caveat verbatim. 'Out of scope: Orky-side emission' section rewritten to 'Scope: Orky-side emission is REAL and shipped' — states the emission is real/shipped/opt-in via config.heartbeat.osc, removes the stale 'dark in production' framing. 'Cross-repo drift caveat' section updated: reason is now documented as free-form prose (not a closed enum), gateM still expected to equal ORKY_PHASES.length (8), FINDING-DA-006's version-redefinition residual cited. CHANGELOG.md [Unreleased]'s orky-osc-heartbeat entry rewritten to describe the real \\x1b]9999;<json>\\x07 contract, opt-in emission, and shipped status. Grepped docs/ + CHANGELOG.md post-edit: zero remaining occurrences of '8888', 'orky=', or the key=value grammar (one unrelated false-positive hex color '#ff8888' in an unrelated terminal-links plan doc, not this contract).",
    "contract_violation": true
  },
  {
    "id": "FINDING-DD-003",
    "lens": "doc-drift",
    "severity": "LOW",
    "gate": "review",
    "location": "tests/shared/orky-heartbeat-status.test.ts:6 (file-header contract-description comment)",
    "claim": "The file-header comment at line 6 still describes `kind` as 'DERIVED in `orkyHeartbeatToPaneStatus` from `needsHuman`/`phase`/`gateN`/`gateM` (REQ-009)' — listing `phase` as a `kind`-derivation input. This iteration's ESC-002/FINDING-DA-004 correction made `kind` done-detection PURELY gate-based (`gateN >= gateM`); `phase` plays no role in `kind` (it only feeds the chip LABEL fallback `?? 'done'`). The SAME iteration added an ITERATION NOTE to this very file (lines 22-28) stating done-detection is 'completely independent of `phase`', so the header now self-contradicts, and line 6 perpetuates the exact stale `phase`-in-kind framing FINDING-DA-004 was raised to correct (now reconciled in src/shared/orky-status.ts's heartbeatKind doc-comment, 02-spec.md REQ-009, 03-plan.md TASK-005, and traceability.{json,md} — but not here).",
    "fix": "At doc-sync (or the next sanctioned touch), reword line 6 to drop `phase` from the `kind` derivation list, e.g. '`kind` is now DERIVED ... from `needsHuman`/`gateN`/`gateM` (REQ-009 — gate-based done-detection; `phase` feeds only the chip LABEL fallback, never `kind`)', matching the corrected wording already used in heartbeatKind's doc-comment and 02-spec.md REQ-009.",
    "phase_of_origin": "tests",
    "status": "open",
    "verified_by": "doc-drift (2026-06-30, loopback iter3 review): confirmed via direct read that tests/shared/orky-heartbeat-status.test.ts:6 lists `phase` among the kind-derivation inputs while the ITERATION NOTE this iteration added at lines 22-28 of the same file states done-detection is completely independent of `phase`; git show HEAD confirms the iteration edited this file (ITERATION NOTE + import additions + TEST-040/041 bodies) without reconciling line 6.",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-009",
    "lens": "devils-advocate",
    "severity": "LOW",
    "gate": "review",
    "location": "src/shared/orky-status.ts:295 (chipLabel `?? 'done'`) + :380 (heartbeatDetail `?? 'done'`) on the stream path / REQ-009 + TEST-041 (tests/shared/orky-heartbeat-status.test.ts:63-67)",
    "claim": "This iteration correctly decoupled `kind`'s done-detection from `phase` (now `gateN >= gateM` alone), but left the `?? 'done'` phase-LABEL fallback in chipLabel/heartbeatDetail unchanged on the stream path — and that fallback's SEMANTICS no longer hold there. On 0004's filesystem path a null `phase` is PRODUCED BY `gateFrontier` and means complete, so labelling it 'done' is correct. On the 0014 stream path a null `phase` comes straight off the wire and (post-fix) is decoupled from completeness, so a feature-bearing heartbeat with `phase: null` but INCOMPLETE gates (`gateN < gateM`) derives `kind: 'busy'` yet renders the self-contradictory chip label `'auth-login · done · 5/8'` and detail `'auth-login: done in progress'` (traced directly: heartbeatKind returns 'busy' for 5>=8 false, while chipLabel/heartbeatDetail's `null ?? 'done'` injects the literal 'done'). This is EXACTLY the shape TEST-041 (`BUSY_PHASE_NULL_HEARTBEAT`, added by this same diff) constructs, yet TEST-041 asserts only `kind === 'busy'` / `not 'done'` and never inspects the label or detail — so the contradictory presentation is a case the tests ignore. Real-wire impact is nil (the emitter's `phase = state.phase || d.phase || null` is null only when `gateFrontier` is null, i.e. all gates passed, so phase-null-with-incomplete-gates does not occur on genuine output), hence LOW — but the spec's stated justification for keeping the fallback ('same chipLabel convention 0004 uses for gateFrontier') silently relies on the emitter invariant 'null phase => complete' that Termhalla never enforces or asserts, so a drifted/spoofed heartbeat (cf. DA-001 trust boundary, DA-006 cross-repo drift) would present a status that reads 'done' while being busy. Distinct from QUAL-003 (which flags only the `?? 'done'` code duplication, not its now-mismatched stream-path semantics), QUAL-005/DD-003 (stale doc/comment TEXT, not rendered output).",
    "fix": "On the stream path, render a null `phase` neutrally for non-done kinds (e.g. omit the phase segment, or show the live gate fraction) rather than the literal 'done', so a busy/incomplete heartbeat never labels itself done; and/or extend TEST-041 to assert the chip label and detail of BUSY_PHASE_NULL do NOT contain 'done'. propose CONV: a derived status label MUST NOT render a completeness word ('done'/'complete') unless the completeness signal it is actually derived from (gate fullness) holds, independent of any single nullable field's incidental value.",
    "phase_of_origin": "implement",
    "status": "open",
    "verified_by": "devils-advocate (2026-06-30, loopback iter3): traced orkyHeartbeatToPaneStatus(BUSY_PHASE_NULL_HEARTBEAT) → heartbeatKind 'busy' (5 >= 8 is false) but chipLabel yields 'auth-login · done · 5/8' and heartbeatDetail yields 'auth-login: done in progress' via `null ?? 'done'`; TEST-041 asserts only `kind`, leaving the label/detail unchecked. Suite green (`npx vitest run tests/shared/orky-heartbeat-status.test.ts` 12/12).",
    "contract_violation": false
  },
  {
    "id": "FINDING-SEC-002",
    "lens": "security",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "tests/main/orky-osc-structural.test.ts:110-128 (TEST-035, rewritten this loopback)",
    "claim": "TEST-035 was rewritten from a frozen sha256 byte-hash of orky-tracker.ts (which failed on ANY byte change, including a legitimate refactor) into a structural regex check on OrkyTracker's public API + a chokidar/node:fs import guard. The replacement is a real, if narrow, loosening of the guard's bypass surface for the exact concern the test exists to catch (0014 creeping a duplicate/competing .orky/ filesystem watch into orky-tracker.ts, the same trust boundary FINDING-DA-002 flags a temporal gap in): its two negative assertions `expect(src!).not.toMatch(/from\\s*['\"]chokidar['\"]/)` and `.not.toMatch(/from\\s*['\"]node:fs/)` only match ESM `from '...'` import syntax. A bare, non-`node:`-prefixed import (`from 'fs'` / `from 'fs/promises'`) or a CommonJS `require('chokidar')` / `require('fs')` would satisfy both regexes while actually introducing the second filesystem-watch mechanism this test is meant to forbid. This is a demonstrable regression in coverage, not a hypothetical: the very same test file's TEST-036 (two describe-blocks below, ~line 136) already adds a third assertion — `expect(parserSrc!).not.toMatch(/require\\(\\s*['\"]chokidar['\"]\\s*\\)/)` — specifically because the `from`-only check is known-incomplete for this concern on a sibling file (orky-osc-parser.ts), but TEST-035 was not given the same treatment for orky-tracker.ts. Currently harmless: orky-tracker.ts (read in full) imports only `node:path`, `@shared/types`, `./find-orky-root`, and `./orky-root-engine`, and delegates all chokidar/fs ownership to the shared OrkyRootEngine (src/main/orky/orky-root-engine.ts) — the gap is in the guard's future-proofing, not in current behavior.",
    "fix": "Mirror TEST-036's `require('chokidar')` check onto TEST-035, and add a negative match for bare (non-`node:`-prefixed) `fs`/`fs/promises` imports and `require('fs')`, so the two sibling import-boundary guards in this file cover the same bypass surface consistently.",
    "phase_of_origin": "review",
    "status": "open",
    "verified_by": "security (loopback iter4, 2026-06-30): confirmed by direct regex trace against tests/main/orky-osc-structural.test.ts:125-128 — `/from\\s*['\"]node:fs/` does not match `from 'fs/promises'` or `from 'fs'` (missing `node:` prefix), and neither `require('chokidar')` nor `require('fs')` is checked at all, unlike the adjacent TEST-036 (lines 131-143 of the same file) which explicitly checks `require('chokidar')` for orky-osc-parser.ts. Read src/main/orky/orky-tracker.ts and src/main/orky/orky-root-engine.ts in full: current code is unaffected (only `node:path` imported in orky-tracker.ts; chokidar/node:fs/promises ownership lives solely in OrkyRootEngine) — this is a guard-coverage gap in the test, not a live vulnerability. No ReDoS/injection risk in the new regexes themselves (all literal patterns over fixed-size source text, no nested quantifiers). Ran `npx vitest run tests/main/orky-osc-structural.test.ts`: 11/11 green — TEST-035 correctly passes against the current, compliant file; this finding concerns what a FUTURE regression could slip past, not present behavior.",
    "contract_violation": false
  },
  {
    "id": "FINDING-QUAL-006",
    "lens": "quality",
    "severity": "LOW",
    "gate": "review",
    "location": "tests/main/orky-osc-structural.test.ts:32 (import statement) — introduced by this loopback's TEST-035 rewrite",
    "claim": "This loopback replaced TEST-035's sha256 byte-hash check of orky-tracker.ts with a structural regex check (see FINDING-SEC-002), which no longer needs `createHash`. The `import { createHash } from 'node:crypto'` at line 32 was left behind and is now dead — `createHash` has zero remaining call sites in the file (verified: it appears exactly once, in the import itself). `readFileSync` (line 30), imported alongside it in the same pre-existing block, is still legitimately used inside `tryRead` (line 37) so it is NOT dead — only the `createHash` import is. Currently non-blocking: `npm run typecheck`'s tests-covering project (`tsconfig.node.json`, which includes `tests/` but does not set `noUnusedLocals`/`noUnusedParameters`) does not enforce unused-import errors, so `tsc --noEmit -p tsconfig.node.json` stays green with this import present (confirmed by running it directly — the only error it reports is a pre-existing, unrelated one in tests/e2e/minimize-resume.spec.ts). It is nonetheless a straightforward dead-code/import-hygiene smell left over from the rewrite, the kind a reader skimming this file's imports would reasonably expect to still be load-bearing (createHash reads as 'this file still hashes something').",
    "fix": "Remove the now-unused `import { createHash } from 'node:crypto'` line from tests/main/orky-osc-structural.test.ts.",
    "phase_of_origin": "review",
    "status": "open",
    "verified_by": "quality (2026-06-30, loopback iter4): grepped the file for `createHash` — one match, the import declaration itself, zero call sites. Ran `npx tsc --noEmit -p tsconfig.node.json` to confirm this does not fail typecheck under the current config (tsconfig.node.json has no noUnusedLocals/noUnusedParameters, unlike tsconfig.json for the renderer) — confirmed non-blocking, filed as a hygiene/maintainability LOW rather than a build-breaking issue.",
    "contract_violation": false
  },
  {
    "id": "FINDING-DA-010",
    "lens": "devils-advocate",
    "severity": "MEDIUM",
    "gate": "review",
    "location": "tests/main/orky-osc-structural.test.ts:113-120 (TEST-035 positive signature assertions) / REQ-013 acceptance",
    "claim": "TEST-035 was rewritten this loopback from a frozen sha256 byte-hash of orky-tracker.ts into source-text regexes, and the replacement is simultaneously TOO WEAK and TOO BRITTLE for the guarantee REQ-013 names first — that 0014 'MUST NOT modify orky-tracker.ts status derivation' (REQ-013 acceptance: 'a code check confirms orky-tracker.ts derivation is unchanged'; 04-tests.md item 6 calls the hash 'intentionally strict', failing on 'even a no-functional-change reformat'). Too weak: the byte-hash failed on ANY change; the new regexes assert only that four method signatures exist (export class OrkyTracker, watch/unwatch/dispose) plus that four import substrings are absent. The status-derivation logic REQ-013 actually protects lives in the method BODIES the test never reads — the engine onStatus fan-out (orky-tracker.ts:44-48), the session-identity supersession re-checks (:58, :65-68), the emit(id,null) clear on unwatch (:75), and detach (:90-97). A future change that rewrote the fan-out precedence, dropped a supersession re-check, or added a side-effect/log-leak inside those bodies while keeping the same four signatures and imports would leave TEST-035 GREEN — yet that is precisely the 'modify status derivation' regression REQ-013 exists to catch. The describe-block and test title still say 'orky-tracker.ts untouched', so a green TEST-035 now sells 'shape + imports unchanged' as 'untouched' — theater relative to the hash it replaced. Too brittle (the opposite failure): the signature regexes hard-code the exact param NAMES (id, cwd) and the explicit async / ': Promise<' / ': void' syntactic form, so a cosmetic refactor — id to paneId, dropping an inferable ': void', or writing watch(): Promise without the async keyword — fails the test RED with zero contract change, reintroducing the very false-positive-loopback class the byte-hash was abandoned to avoid, just on a smaller surface. Distinct from FINDING-SEC-002 (which flags only the import guard's false-NEGATIVE bypass via require()/bare fs) and FINDING-QUAL-006 (dead createHash import): this is about the positive signature assertions and the overall weakening/mislabeling of REQ-013's freeze. Current orky-tracker.ts is fully compliant (zero src/ diff this cycle), so this is a future-regression-detection weakness, not a live violation — hence MEDIUM, not HIGH.",
    "fix": "Restore a guard that matches REQ-013's 'derivation is unchanged' intent. Best: pin 0014's non-modification of orky-tracker.ts with a feature-scoped git-diff check (assert the file is absent from THIS feature's changed-file set) instead of a content freeze, so a legitimate sibling-feature refactor never surfaces as a 0014 false positive. Failing that, either (a) re-baseline the sha256 to the current post-0005 file — this restores the exact 'any byte change fails' strength and is a one-line update owned by whoever next touches the file, or (b) if keeping a structural check, loosen the two param matches to arity only (accept any identifier + optional/omitted return-type annotation, not the literal names id/cwd and not a mandatory async keyword) to kill the rename/reformat brittleness, AND import and call OrkyTracker to assert the actual behavior contract the human's fix description named ('arity/behavior contract') — e.g. watch() returns a Promise and unwatch() triggers a null emit — so the derivation, not merely its shape, is verified. propose CONV: a frozen/golden structural-or-hash test that pins a SHARED source file's contract MUST be co-owned by (live in, or be updated atomically at merge by) whichever feature modifies that file; a content-freeze test on a file other features also touch, parked in one feature's frozen suite, has no atomic cross-feature update path and surfaces every legitimate refactor as a false-positive loopback in an unrelated feature.",
    "phase_of_origin": "tests",
    "status": "open",
    "verified_by": "devils-advocate (loopback, 2026-06-30): read the rewritten TEST-035 (tests/main/orky-osc-structural.test.ts:101-129) and orky-tracker.ts in full. Confirmed the four positive assertions match only 'export class OrkyTracker', the exact 'async watch(id: string, cwd: string): Promise<' form, and 'unwatch(id: string): void' / 'dispose(): void' — the method bodies carrying the status derivation (fan-out :44-48, supersession :58/:65-68, unwatch null-emit :75, detach :90-97) are matched by nothing, so a body-only derivation change passes green. Confirmed the param-name/async/return-type literals make the regexes fail RED on a harmless rename or annotation drop (the same false-positive class the hash was dropped for). Cross-checked 04-tests.md item 6 (hash was 'intentionally strict', failing on any reformat) and the existing ledger: not a duplicate of SEC-002 (import-guard bypass) or QUAL-006 (dead import). REQ-013's product MUST is not violated by current code (zero src/ diff), so contract_violation=false and this does not block; recorded as a test-adequacy/coverage regression on a secondary freeze, while REQ-013's substantive precedence guarantee stays fully pinned by TEST-032/033/034.",
    "contract_violation": false
  },
  {
    "id": "FINDING-DD-004",
    "lens": "doc-drift",
    "severity": "MEDIUM",
    "gate": "review",
    "location": ".orky/features/0014-orky-osc-heartbeat/04-tests.md:102, :121-122, :231-236, :240-241, :258-260 (+ tests/main/orky-osc-structural.test.ts:23)",
    "claim": "This loopback rewrote TEST-035 from a frozen sha256 byte-hash of orky-tracker.ts into a structural public-API/import-boundary check, but the tests-gate artifact 04-tests.md was not resynced and now misdescribes TEST-035 in five places. Most misleading: item 6 (04-tests.md:231-236) still reproduces the now-obsolete golden hash literal `aba940393d50df9e658c0930aadfd136287984f89e63713d423c3d45a93aa526` and asserts 'REQ-013's orky-tracker.ts is unchanged acceptance is pinned with a frozen sha256 hash ... Any byte-level edit ... fails this FROZEN test, intentionally strict', i.e. it documents the ABANDONED approach as the current guard. The git diff shows that hash literal was deleted from the .ts test file this cycle, so 04-tests.md:233 is now its ONLY surviving copy in the repo. Also stale: :102 ('REQ-013 (orky-tracker.ts byte-for-byte unchanged via a frozen sha256 hash)'), :121-122 and :258-260 (both cite 'TEST-035's frozen-hash pattern'/'frozen source hash' as the precedent justifying TEST-042/043's placement in this file — a rationale that no longer holds), and :240-241 (lists 'the orky-tracker.ts hash' among the pre-implementation-passing structural properties). Separately, the rewritten test file self-contradicts: tests/main/orky-osc-structural.test.ts:23 still calls the file's established style 'TEST-035's frozen-hash' while the same file's lines 26-28 and 101-109 explain the hash was replaced. Distinct from DA-010 (TEST-035 guard too weak/brittle), SEC-002 (import-guard require()/bare-fs bypass), QUAL-006 (dead createHash import), and DD-001/DD-002/DD-003 (state.json testIds block / present-tense RED header comments / phase-in-kind header) — none of those flag 04-tests.md's stale frozen-hash prose or the orphaned golden-hash literal.",
    "fix": "At doc-sync (or the next sanctioned touch), rewrite 04-tests.md's TEST-035 descriptions (:102, :121-122, :231-236, :240-241, :258-260) to the structural public-API + chokidar/node:fs import-boundary check the test now performs, DELETE the obsolete golden-hash literal aba940393d50df9e658c0930aadfd136287984f89e63713d423c3d45a93aa526 (the only remaining copy in the repo), and reword the 'frozen-hash pattern' precedent references used to justify TEST-042/043's placement. Also fix the self-contradictory comment at tests/main/orky-osc-structural.test.ts:23 ('TEST-035's frozen-hash') to match the file's own updated TEST-035 rationale. propose CONV: when a loopback changes a TEST's implementation approach, the same pass MUST resync every prose description of that TEST (04-tests.md, its own header/inline comments, traceability notes) — a rewritten test whose gate artifact still describes the abandoned approach, especially one quoting a now-dead golden hash value, is doc-drift.",
    "phase_of_origin": "tests",
    "status": "resolved",
    "verified_by": "doc-drift (2026-06-30, TEST-035 rewrite loopback): grepped the repo for the golden hash aba940393d50df9e658c0930aadfd136287984f89e63713d423c3d45a93aa526 — it now appears ONLY at 04-tests.md:233 (git diff of tests/main/orky-osc-structural.test.ts confirms the literal was deleted from the .ts file this cycle). Read 04-tests.md items 6/7/10 plus lines 102 and 121-122: all still frame TEST-035 as a 'frozen sha256 hash'/'frozen source hash', and item 6 still calls it the current, intentionally-strict FROZEN guard. Cross-checked state.json's loopbacks[2] (to:tests, from:doc-sync) — that entry ACCURATELY records the byte-hash→structural swap, so the loopback bookkeeping itself is NOT drifted; traceability.json:170 / traceability.md:39 list TEST-035 only as an unchanged test id under REQ-013 (correct, test id did not change). Confirmed the feature doc docs/features/orky-osc-heartbeat.md and CHANGELOG.md carry no TEST-035 reference. Confirmed not a duplicate of DA-010/SEC-002/QUAL-006/DD-001/DD-002/DD-003. phase_of_origin=tests (shift-left): the fresh test-designer rewrote TEST-035 in this loopback's tests phase but left the tests-gate artifact un-synced — same origin convention as DD-001/DD-002/DD-003. RESOLVED doc-sync (2026-06-30): rewrote all five flagged 04-tests.md locations (the file-table REQ-013 cell, the doc-sync-closeout precedent references for TEST-042/043, design note 6, design note 7's pre-implementation-passes list) to describe TEST-035 as the structural public-API/import-boundary check it now is, and deleted the obsolete golden-hash literal `aba940393d50df9e658c0930aadfd136287984f89e63713d423c3d45a93aa526` (confirmed by grep: zero remaining occurrences in 04-tests.md). Added a new '## TEST-035 loopback' section recording the false-positive-hash rationale for posterity. Did NOT touch tests/main/orky-osc-structural.test.ts:23's self-contradictory 'frozen-hash' comment — that is reserved for a dedicated orky:test-designer dispatch per this cycle's explicit instructions (out-of-scope follow-up, noted in the doc-sync report, not left silently unaddressed).",
    "contract_violation": false
  }
]
